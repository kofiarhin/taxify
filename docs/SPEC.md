# Legacy Spec Notes

The main workflow now stores request-specific specs in `_spec/`.

Use this file only for durable product notes that apply across many requests. Do not treat this file as the active implementation spec.

Active specs must be saved as timestamped or slugged files such as:

```txt
_spec/2026-05-10-add-dark-theme.md
```

## Durable Product Notes

- Project: `<PROJECT_NAME>`
- Owner: `<OWNER_OR_TEAM>`
- Product summary: `<ONE_SENTENCE_PRODUCT_SUMMARY>`

## Standing Goals

- `<Goal that applies across requests>`

## Standing Constraints

- `<Constraint that applies across requests>`

## Standing Non-Goals

- `<Out-of-scope item that applies across requests>`
