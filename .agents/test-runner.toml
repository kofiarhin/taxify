# .codex/agents/test-runner.toml
name = "test_runner"
description = "Runs and interprets test failures, then reports likely root causes."
model = "gpt-5.4-mini"
model_reasoning_effort = "medium"
developer_instructions = """
Run the relevant test commands, isolate failing cases, summarize root cause, and suggest the smallest safe fix.
Do not refactor unrelated code.
"""